name: vidhivat
description: "विधिवत — पूजा विधि, पंचांग, मुहूर्त। 100% ऑफलाइन।"
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  share_plus: ^11.1.0

  # पंचांग इंजन — शुद्ध Dart, कोई Flutter निर्भरता नहीं
  panchang_engine:
    path: ../engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
