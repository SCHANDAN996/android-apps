<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>
    <!-- ⚠️ USE_EXACT_ALARM जान-बूझकर नहीं लिया गया — Google Play उसे सिर्फ़
         अलार्म-घड़ी / कैलेंडर ऐप को देता है। हमारा रिमाइंडर उसके बिना भी
         चलता है (permission न मिले तो notification_service अपने-आप inexact
         पर चला जाता है, बस 1-2 मिनट आगे-पीछे हो सकता है)। -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ग्राहक जोड़ते समय "कॉन्टैक्ट से चुनें" के लिए (सिर्फ़ पढ़ना, लिखना नहीं) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <application
        android:label="Pro Kisan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- Disable Impeller: on some devices Flutter's new renderer causes a
             black screen / crash on launch. This forces the stable Skia renderer. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- ⚠️ Android 11 (API 30) से: ऐप को पहले बताना पड़ता है कि वह किन दूसरे
             ऐप को खोलना चाहता है। ये न लिखे हों तो canLaunchUrl() हमेशा false
             लौटाता है — चाहे फ़ोन में Gmail/Chrome/WhatsApp लगे ही क्यों न हों।
             इसी वजह से ईमेल, "पूरा पढ़ें", WhatsApp शेयर और कॉल — चारों चुपचाप
             काम करना बंद कर देते थे। -->

        <!-- वेबसाइट खोलना (खबर का लिंक, योजना का सरकारी पोर्टल) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>

        <!-- ईमेल भेजना -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>

        <!-- फ़ोन लगाना (ग्राहक, हेल्पलाइन) -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>

        <!-- टेक्स्ट/लिंक शेयर करना -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- WhatsApp — निजी और business, दोनों -->
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
    </queries>
</manifest>
